<form method="post" action="{{ path('app_assignment_delete', {'idAssignment': assignment.idAssignment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assignment.idAssignment) }}">
    <button class="btn">Delete</button>
</form>
