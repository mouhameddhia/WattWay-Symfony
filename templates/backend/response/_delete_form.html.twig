<form method="post" action="{{ path('app_response_delete', {'idResponse': response.idResponse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ response.idResponse) }}">
    <button class="btn">Delete</button>
</form>
